replicaCount: 1
image:
  repository: mysql
  tag: latest
service:
  type: ClusterIP
  port: 3306
resources: {}
pvc:
  name: mysql-local-pvc
mysqlRootPassword: "rootpassword"
mysqlDatabase: "wordpress"
mysqlUser: "wpuser"
mysqlPassword: "wppassword"

exporter:
  enabled: true
  tag: latest
  user: "wpuser"
  password: "wppassword"
